import { FollowCursorDirective } from './follow-cursor.directive';

describe('FollowCursorDirective', () => {
  it('should create an instance', () => {
    const directive = new FollowCursorDirective();
    expect(directive).toBeTruthy();
  });
});
